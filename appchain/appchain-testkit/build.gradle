dependencies {
    api project(':runtime')
    api libs.junit.jupiter.api

    implementation libs.cardano.client.crypto
    implementation libs.slf4j.api
}

java {
    sourceCompatibility = 25
    targetCompatibility = 25
}

def publishedArtifactId = 'yano-appchain-core-testkit'

tasks.named('jar') {
    archiveBaseName = publishedArtifactId
}

tasks.named('compileJava') {
    // This module has no annotation processor that consumes the repository-wide
    // project identity option, so do not emit an irrelevant javac processing warning.
    options.compilerArgs.removeAll { it.startsWith('-Aproject=') }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = publishedArtifactId
            pom {
                name = 'Yano App-Chain Core Testkit'
                description = 'OrderedLog, host SPI, and plugin conformance support for Yano'
            }
        }
    }
}
