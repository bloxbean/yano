package com.bloxbean.cardano.yano.appchain.config;

/** How completely tooling metadata represents a runtime-owned namespace. */
public enum ValidationCoverage {
    FULL,
    PARTIAL,
    UNSUPPORTED_METADATA
}
