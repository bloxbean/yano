package com.bloxbean.cardano.yano.appchain.config;

/** Scalar/container representations accepted by the offline app-chain config validator. */
public enum PropertyType {
    BOOLEAN,
    INTEGER,
    LONG,
    STRING,
    STRING_LIST,
    OBJECT
}
