dependencies {
    api project(':core-api')
}

java {
    sourceCompatibility = 25
    targetCompatibility = 25
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yano App-Chain Configuration'
                description = 'Typed app-chain configuration metadata and offline validation contracts'
            }
        }
    }
}
