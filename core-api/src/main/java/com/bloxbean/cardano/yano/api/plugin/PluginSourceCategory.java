package com.bloxbean.cardano.yano.api.plugin;

/** Where a catalog bundle was discovered without revealing a local path. */
public enum PluginSourceCategory {
    CLASSPATH,
    DIRECTORY,
    NATIVE
}
