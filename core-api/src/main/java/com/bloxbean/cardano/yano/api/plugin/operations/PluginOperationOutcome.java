package com.bloxbean.cardano.yano.api.plugin.operations;

/** Fixed host-observed outcomes for plugin operation counters. */
public enum PluginOperationOutcome {
    SUCCEEDED,
    FAILED,
    REJECTED,
    TIMED_OUT
}
