package com.bloxbean.cardano.yano.api.plugin.operations;

/** Fixed host-observed plugin operation dimensions. */
public enum PluginOperation {
    ACTIVATION,
    LIFECYCLE,
    HEALTH_SAMPLE,
    METRICS_SAMPLE,
    DOMAIN_GET,
    DOMAIN_POST
}
