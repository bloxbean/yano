package com.bloxbean.cardano.yano.api.plugin;

/** Secret-free operator-policy outcome for one structurally valid bundle. */
public enum PluginSelectionStatus {
    SELECTED,
    DENIED,
    NOT_ALLOW_LISTED
}
