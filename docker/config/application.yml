quarkus:
  http:
    host: 0.0.0.0
    port: 7070
  log:
    file:
      enabled: true
      path: /app/logs/yano.log

yano:
  storage:
    path: /app/chainstate

yaci:
  plugins:
    directory: /app/plugins
