services:
  yano:
    environment:
      YANO_PROFILE: ${YANO_PROFILE:-mainnet}
      YANO_NETWORK: ${YANO_NETWORK:-mainnet}
    volumes:
      - ${YANO_CHAINSTATE_PATH:-../chainstate-mainnet}:/app/chainstate
      - ${YANO_APPCHAIN_STATE_PATH:-../appchain-state-mainnet}:/app/appchain-state
