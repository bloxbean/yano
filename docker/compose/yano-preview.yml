services:
  yano:
    environment:
      YANO_PROFILE: ${YANO_PROFILE:-preview}
      YANO_NETWORK: ${YANO_NETWORK:-preview}
    volumes:
      - ${YANO_CHAINSTATE_PATH:-../chainstate-preview}:/app/chainstate
